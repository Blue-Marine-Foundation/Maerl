// TODO: Once backend is ready, design and implement Unit updates list
export default async function UnitOutputsPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  return (
    <div>
      <p>Unit outputs and output indicators to be displayed here</p>
    </div>
  );
}
