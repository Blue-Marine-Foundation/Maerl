// TODO: Once backend is ready, design and implement Unit updates list
export default async function UnitUpdatesPage({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  return (
    <div>
      <p>Unit updates to be displayed here</p>
    </div>
  );
}
